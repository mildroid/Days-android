package com.mildroid.days.utils

import com.mildroid.days.BuildConfig

const val BASE_UNSPLASH_URL = "https://api.unsplash.com/"
const val CONTENT_TYPE = "Content-Type"
const val APPLICATION_JSON = "application/json"
const val ACCEPT_VERSION = "Accept-Version"

const val APP_DATABASE_NAME = BuildConfig.APPLICATION_ID + ".app_database_name"

const val APP_SETTINGS_PREFERENCES = BuildConfig.APPLICATION_ID + ".app_settings_preferences"
const val APP_TEMP_PREFERENCES = BuildConfig.APPLICATION_ID + ".app_temp_preferences"
const val APP_ENTRY_STATE = BuildConfig.APPLICATION_ID + ".app_entry_state"

const val ADD_EVENT_INITIAL_DATE = BuildConfig.APPLICATION_ID + ".AddEventActivity.initial_date"
const val MAIN_VIEW_TYPE = BuildConfig.APPLICATION_ID + ".MainActivity.view_type"
const val TEMPORARY_EVENT_TITLE = BuildConfig.APPLICATION_ID + ".addEventActivity.temporary_event_title"
const val TEMPORARY_EVENT_DATE = BuildConfig.APPLICATION_ID + ".addEventActivity.temporary_event_date"
const val TEMPORARY_EVENT_PHOTO = BuildConfig.APPLICATION_ID + ".addEventActivity.temporary_event_photo"

const val EVENT_VIEW_TYPE = BuildConfig.APPLICATION_ID + ".eventActivity.event_view_type"
const val EVENT_ID = BuildConfig.APPLICATION_ID + ".eventActivity.event_id"
const val EVENT_TITLE = BuildConfig.APPLICATION_ID + ".eventActivity.event_title"
const val EVENT_DATE = BuildConfig.APPLICATION_ID + ".eventActivity.event_date"
const val EVENT_IMAGE = BuildConfig.APPLICATION_ID + ".eventActivity.event_image"

const val EVENT_OPTIONS_SHEET = BuildConfig.APPLICATION_ID + ".eventActivity.event_options_bottom_sheet"
