<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Days" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimary</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/white</item>
        <item name="colorSecondaryVariant">@color/white</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:windowTranslucentStatus">true</item>
        <item name="fontFamily">@font/product_sans_regular</item>
        <item name="bottomSheetDialogTheme">@style/RoundedBottomSheetDialog</item>

<!--        <item name="android:windowContentTransitions">true</item>-->
    </style>

<!--
    <style name="Theme.MainTransition" parent="Theme.Days" tools:targetApi="lollipop">
        <item name="android:windowSharedElementEnterTransition">@transition/change_image_transform
        </item>
        <item name="android:windowSharedElementExitTransition">@transition/change_image_transform
        </item>
    </style>
-->

    <style name="Theme.Days.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/semi_black</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_days_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.Days</item>
    </style>


</resources>